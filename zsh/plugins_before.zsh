#!/usr/bin/env zsh

# External plugins (initialized before)

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(git colored-man-pages compleat copybuffer dirhistory docker docker-compose docker-machine dotenv dotnet emoji github gpg-agent jira kubectl kubectx minikube node npm nvm pipenv sudo virtualenv vscode zsh-autosuggestions zsh-syntax-highlighting)