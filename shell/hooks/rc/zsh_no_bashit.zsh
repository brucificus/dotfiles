#!/usr/bin/env zsh


unset BASH_IT
unset BASHIT_ALIASES_AVAILABLE
unset BASHIT_COMPLETIONS_AVAILABLE
unset BASHIT_PLUGINS_AVAILABLE

unset BASH_IT_LOG_PREFIX
unset BASH_IT_CUSTOM
unset CUSTOM_THEME_DIR
unset BASH_IT_BASHRC

unset _bash_it_library_finalize_hook
